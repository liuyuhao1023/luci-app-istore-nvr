# iStore NVR - 阶段一：软路由运行环境检查与技术评估报告

## 一、目标软路由实际硬件与运行环境明细

| 检查项 | 实际环境参数 | 评估与结论 |
| :--- | :--- | :--- |
| **设备 IP** | `192.168.1.15` (br-lan) | 局域网通信正常，延迟 < 1ms |
| **操作系统** | iStoreOS 25.12.5 (基于 OpenWrt) | 固件构建版本 2026091113 |
| **Linux 内核** | `6.12.94 #0 SMP x86_64` | 最新 6.12 LTS 内核，系统与网络协议栈现代 |
| **CPU 规格** | Intel Celeron J3455 @ 1.50GHz (4 核 4 线程) | x86_64 Apollo Lake 架构，低功耗 x86 处理器 |
| **物理内存** | **7.61 GB (7,977,360 KB)** | 可用内存 **7.20 GB (7,551,788 KB)**，内存极为充裕！ |
| **Swap 交换区** | 0 MB (未启用) | 内存充足，无需频繁换页 |
| **系统分区 (/overlay)** | ext4，总容量 1.9G，已用 19.4M，**剩余 1.9G** | **容量有限，绝对严禁写入录像和海量日志** |
| **本地数据磁盘** | SATA SSD 128GB，挂载于 `/mnt/sata1-4` | **剩余可用 108.6 GB**，格式 ext4，可作为本地测试存储 |
| **网卡与网络带宽** | 双口 Intel I226-IT 2.5Gbps 网卡 (`eth0`, `eth1`) | 链路硬件性能极高（2.5G），支持大码流录像高并发网络吞吐 |
| **已安装运行环境** | **Docker 27.3.1 (已就绪)**、Containerd | 原生环境无 Go / Python / FFmpeg / Node / SQLite3 命令 |
| **SMB/CIFS 支持** | **内核 cifs.ko 已加载，mount.cifs 已安装** | **完美原生支持 SMB/CIFS 挂载，无须改动内核** |
| **GPU / 硬件加速** | Intel HD Graphics 500 (`/dev/dri/card0`, `renderD128`) | 核显节点正常加载，支持 Intel VA-API 硬件编解码 |
| **现有 Web 服务端口** | 80/443 (uhttpd), 7681 (ttyd), 22 (dropbear), 445/139 (Samba) | 常用流媒体与 NVR 后台端口（如 8080/8554/8889 等）完全空闲 |

---

## 二、32～64 路性能与资源容量评估

针对用户提出的 **32～64 路摄像头** 管理与录像目标，结合 Intel J3455 的算力与 I/O 限制，做出明确的界限划分：

### 1. 摄像头管理数量（上限 64 路）
* **结论：完全可行**。
* **分析**：摄像头列表、设备元数据、ONVIF 状态轮询、在线/离线健康心跳检测等，由 Go 后端通过非阻塞协程并发处理，64 路的心跳轮询对 CPU 占用 < 2%，内存占用 < 30MB。

### 2. 同时录像数量（设计支持 32～64 路直通录像）
* **录像码率模型**：
  * 单路 1080P H.264/H.265 主码流：平均码率 2~4 Mbps (约 250KB/s ~ 500KB/s)。
  * **32 路同时录像**：总网络写入带宽约 **64 ~ 128 Mbps (8 ~ 16 MB/s)**。
  * **64 路同时录像**：总网络写入带宽约 **128 ~ 256 Mbps (16 ~ 32 MB/s)**。
* **硬件瓶颈评估**：
  * **网络接口**：双 Intel I226-IT 2.5G 网卡，局域网吞吐上限达 2500 Mbps，32 MB/s 仅占用其带宽的 10%，网络无压力。
  * **CPU 消耗**：**必须严格采用“码流直通复制（Stream Pass-through / Demux-Remux）”**，直接将 RTSP 流封装为 fMP4/TS 片段写入磁盘，不解压不重编码。J3455 的 4 个核心处理 32~64 路网络包解封装及内存缓冲，预计占用 15%~35% CPU，处于安全负载范围内。
  * **存储瓶颈与写策略**：
    * 本地 SSD 仅剩 108GB，若 32 路以 16MB/s 写入，**1.8 小时即会写满！** 因此**本地磁盘仅适合少量临时调试，32~64 路实际部署必须依赖局域网 SMB NAS 或外接大容量硬盘**。
    * 64 路并发写入磁盘必须在应用层使用 **内存聚合缓冲（Ring Buffer）**，避免 64 个线程同时发起微小 I/O 导致磁盘队列卡死。

### 3. 同时实时预览数量（区分单画面主码流与多画面子码流）
* **流复用原则**：流媒体分发网关拉取一次摄像头 RTSP 流，同时供给录像与多路预览，避免多次连接摄像头压垮摄像头 CPU。
* **多画面策略**：
  * 4/9/16/25/36/64 宫格预览：**强制默认使用摄像头子码流（Sub-stream，D1/CIF 分辨率，0.5Mbps）**。
  * 单画面放大/全屏：按需切换为主码流。
  * 浏览器解码压力：64 画面同时渲染对客户端 PC/浏览器的 GPU 要求极高，服务端通过子码流分发将单机带宽压降在 30Mbps 以内。

### 4. 同时转码数量（严格限制）
* **结论：严禁默认转码，不可用于全量录像**。
* Apollo Lake HD 500 核显仅能承受 **1~2 路 1080P 实时硬件转码**（若使用 CPU 软解则会直接 100% 满载导致软路由路由功能卡死）。
* 系统必须坚持“码流原样直通”。若部分浏览器不支持 H.265，优先通过 WebCodecs/MSE 前端软解播放子码流，或引导用户使用兼容客户端。

---

## 三、推荐技术架构设计

```
+---------------------------------------------------------------------------------+
|                                 前端管理层                                      |
|          Vue 3 + Vite + Element Plus + WebRTC / MSE / WebCodecs 播放器           |
|            - 1/4/9/16/25/36/64 宫格布局，子码流默认，主码流单屏放大              |
|            - 录像独立开关、存储管理、SMB 图形化配置、时间轴回放                  |
+----------------------------------------+----------------------------------------+
                                         | HTTP REST API / WebSocket
+----------------------------------------v----------------------------------------+
|                            iStore NVR 核心服务 (Go)                             |
|  +---------------------------------------------------------------------------+  |
|  | 设备适配层 (Camera Adapter): Hikvision Adapter (ONVIF / RTSP / ISAPI)     |  |
|  +---------------------------------------------------------------------------+  |
|  | 流处理引擎 (Media Engine): 统一 RTSP 会话复用，WebRTC/Wasm 实时转发        |  |
|  +---------------------------------------------------------------------------+  |
|  | 录像调度引擎 (Recording Engine): 独立线程，直通切片 (fMP4/TS)，环形缓冲写  |  |
|  +---------------------------------------------------------------------------+  |
|  | 存储与索引管理 (Storage & SQLite):                                         |  |
|  |   - 数据库：SQLite 嵌入式存储于数据盘                                      |  |
|  |   - 本地盘：/mnt/sata1-4                                                   |  |
|  |   - 网络盘：Linux 原生 CIFS 安全挂载 (/mnt/nvr/storage/nas01)               |  |
|  +---------------------------------------------------------------------------+  |
+---------------------------------------------------------------------------------+
```

### 1. 部署模式选型
* **选型结论**：**Go 纯静态编译单二进制程序 或 极轻量 Docker 容器**。
  * 鉴于软路由已内置完整运行的 Docker，且环境清洁，开发与测试阶段推荐采用独立二进制程序挂载于 `/mnt/sata1-4/nvr` 直接运行，极度省资源且不污染系统；发布时同时产出 iStoreOS 原生 opkg 包和 Docker 镜像。
* **持久化目录设计**：
  * 程序与配置：`/mnt/sata1-4/istore-nvr/`
  * 数据库与索引：`/mnt/sata1-4/istore-nvr/data/nvr.db`
  * 录像目录：`/mnt/sata1-4/recordings/` 或 SMB 挂载目录 `/mnt/nvr/storage/<nas_id>/`
  * 保护机制：检查挂载点，非挂载状态下拒绝向根目录或临时目录写入录像。

---

## 四、下一步行动计划（阶段一技术验证）

1. **海康威视摄像头连接验证**：
   - 验证海康威视摄像头的 ONVIF 发现与鉴权。
   - 验证海康威视主码流与子码流的 RTSP 稳定拉取。
   - 验证原始码流直通保存为 fMP4/TS 录像切片及无损回放。
